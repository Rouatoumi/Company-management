{% extends 'layouts/master.html.twig' %}

{% block title %}Affichage RendezVous 
{% endblock %}

{% block content %}
	<h1>RendezVous </h1>

	<table class="table table-stripped table-hover">
		<thead>
			<tr>
				<th>Id</th>
				<th>Date</th>
				<th>Titre</th>
				<th>Description</th>
				<th>actions</th>
			</tr>
		</thead>
		<tbody>
			{% for rendez_vou in rendez_vouses %}
				<tr>
					<td>{{ rendez_vou.id }}</td>
					<td>{{ rendez_vou.date ? rendez_vou.date|date('Y-m-d') : '' }}</td>
					<td>{{ rendez_vou.titre }}</td>
					<td>{{ rendez_vou.description }}</td>
					<td>
						<button class="btn btn-outline-primary">
							<a href="{{ path('rendez_vous_show', {'id': rendez_vou.id}) }}">show</a>
						</button>
						{% if is_granted('ROLE_ADMIN') %}

							<button class="btn btn-outline-primary">
								<a href="{{ path('rendez_vous_edit', {'id': rendez_vou.id}) }}">edit</a>
							</button>
						{% endif %}

					</td>
				</tr>
			</td>
		</tbody>
	</tr>
{% else %}
	<tr>
		<td colspan="5">no records found</td>
	</tr>
{% endfor %}</tbody></table>{% if is_granted('ROLE_ADMIN') %}<button class="btn btn-outline-primary">
<a href="{{ path('rendez_vous_new') }}">Create new</a></button>{% endif %}{% endblock %}
